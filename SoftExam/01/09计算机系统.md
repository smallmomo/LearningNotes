
# 1.原码 反码 补码
---
## 一、基础规定
1. 最高位：**符号位**
- 正数：符号位 = \(0\)
- 负数：符号位 = \(1\)
2. 正数：**原码 = 反码 = 补码**（三个一模一样）
3. 只有**负数**，三者不一样（考试核心考点）

---
## 二、三者定义 & 换算公式
设：机器字长n位

### 1. 正数（\(X>0\)）
原码 = 反码 = 补码

### 2. 负数（\(X<0\)）
1. **原码**：符号位1，数值位直接写真值
2. **反码**：
   负数反码 = 符号位不变，数值位按位取反
3. **补码**（最重要！计算机存的都是补码）
   负数补码 = 反码 + 1

---
## 三、实操计算举例（8位）
### 例1：正数 +5
真值：\(+5 = 00000101\)
- 原码：00000101
- 反码：00000101
- 补码：00000101

---
### 例2：负数 -5
1. 原码：
   符号位1 + 5二进制
   10000101
2. 反码：符号不变，数值位取反
   11111010
3. 补码：反码
   11111011


---
## 四、特殊值 0（必考坑点）
8位：
1. 原码、反码：0 有两种表示
- 正0：\(00000000\)
- 负0：\(10000000\)
2. 补码：**0 只有一种**
00000000  
   👉 补码好处：没有负0，多存一个最小负数

---
##  五、8位范围（选择常考）
- 原码 / 反码：-127 ~ +127
- 补码：-128 ~ +127
  （多出来的 -128 就是利用负0编码位置）

---
## 六、最简秒杀口诀
1. 正数：三码相同
2. 负数：
- 原码：符号1，原样写
- 反码：符号不动，数字取反
- 补码：反码 \(+1\)
3. 计算机**运算、存储一律用补码**

# 2. 浮点数相加运算规则

浮点数加法运算规则
浮点数格式：
![img_15.png](img_15.png)

## 四步标准流程
### 第一步：对阶
![img_16.png](img_16.png)
2. **小阶向大阶看齐**
   阶码小的那个，**尾数右移**ΔE位，直到阶码相等。
> 原则：**只右移、不左移，小阶追大阶**

### 第二步：尾数相加
对阶完成后，两个**尾数直接做二进制加减**。

### 第三步：规格化
相加后尾数要变回**规格化浮点数**
- 二进制规格化：尾数满足 0.1xxxx⋯
1. **左规**：尾数太小（高位是0）→ 尾数左移，阶码减1
2. **右规**：尾数溢出（≥1）→ 尾数右移，阶码加1

### 第四步：舍入 + 判溢出
1. 右移丢掉的低位，做**舍入处理**（0舍1入常用）
2. 检查阶码是否超出范围，判断**溢出**

---

## 一句话口诀
**对阶→尾数加→规格化→舍入判溢出**
对阶：小阶右移追大阶；
规格化：左规缩尾数降阶，右规扩尾数升阶。



## 浮点数标准格式
浮点数：  
![img_17.png](img_17.png)
- \(M\)：尾数（纯小数，规格化：
  0.1xxxxx
  ）
- \(E\)：阶码



---

## 举例实操
设两个二进制浮点数：  
![img_18.png](img_18.png)  
阶码用二进制，先转十进制：

![img_19.png](img_19.png)

## 第1步：对阶
![img_20.png](img_20.png)

## 第2步：尾数相加
![img_21.png](img_21.png)
## 第3步：规格化
二进制规格化要求：**尾数 0.1xxxx**
现在尾数 \(0.10011\) 已经符合，**不用左规、右规**。

## 第4步：舍入 & 溢出
本例尾数刚好5位，无多余位，不用舍入；
阶码在合法范围，无溢出。

---

## 最终结果
![img_22.png](img_22.png)

---

## 补充：什么是左规、右规
1. **右规**
   尾数 ≥ 1 （形如 1.0xxxx）
   → 尾数右移一位，阶码+1
2. **左规**
   尾数太小 形如 0.00xxxxx
   → 尾数左移，阶码-1，直到变成 0.1xxxx



# 3.磁盘容量的计算

硬盘容量分为非格式化容量和格式化容量两种，计算公式如下：
非格式化容量=面数×（磁道数/面）×内圆周长×最大位密度
格式化容量=面数×（磁道数/面）×（扇区数/道）×（字节数/扇区）

实例
假设某硬盘由5个盘片构成（共有8个记录面），盘面有效记录区域的外直径为30cm，内直径为10cm，记录位密度为250位/mm，磁道密度为16道/mm，每磁道分16个扇区，每扇区512字节，则该硬盘的格式化容量约为（ ）MB。  
题目中给出硬盘的面数为8，每面的磁道数为（30-10）×10÷2 ×16，每磁道扇区数为16，每扇区512字节，
因此其格式化容量为
![img_31.png](img_31.png)

每面的磁道数为（30-10）×10÷2 ×16  为啥是这样计算呢

先把**每一步拆碎讲懂**，你就明白为什么是这么算了。

---

## 第一步：先理已知条件
- 外直径：30 cm
- 内直径：10 cm
- 磁道密度：**16 道/mm**
- 一共 8 个记录面

### 先统一单位：cm 转 mm
1 cm=10 mm

---

## 第二步：求盘面「径向有效宽度」

![img_32.png](img_32.png)

有效径向宽度（圆环宽度）：
15−5=10 cm=10×10=100 mm


---

## 第三步：为什么磁道数 
![img_33.png](img_33.png)

![img_34.png](img_34.png)
---

## 第四步：顺便帮你算出 格式化容量（完整算一遍）

已知：
- 记录面：8 面
- 每面磁道：1600 道
- 每磁道 16 扇区
- 每扇区 512 B


# 4. 补码为什么比 其他的多一个数呢
先记结论：
**原码、反码：0 有 +0、-0 两个编码**
**补码：把 +0、-0 合并成同一个 0，空出来的编码多表示一个最小负数**

---

## 一、先拿 1 字节 **8位二进制** 举例
8位最高位是符号位：
- 0 开头：正数
- 1 开头：负数

### 1. 原码范围
原码：
- 正数：`0000 0000` ~ `0111 1111`
- 负数：`1000 0000` ~ `1111 1111`

关键：
- `0000 0000` → **+0**
- `1000 0000` → **-0**
  ✅ **0 占了两个编码**

原码实际范围：**-127 ~ +127**

---

### 2. 反码范围
反码同样：
- `0000 0000` → +0
- `1111 1111` → -0
  也是**两个0**
  范围照样：**-127 ~ +127**

---

### 3. 补码最关键：0 只存一个
补码规则：
**没有 -0 ！**
所有0 统一用：`0000 0000` 表示

那原本 `1000 0000` 这个编码**空出来了**，干嘛？
👉 拿来多表示一个**最小负数**

8位补码：
- `1000 0000` 规定代表 **-128**
  正数到 `0111 1111` = **+127**

所以补码范围：
**-128 ~ +127**

---

## 二、为啥多一个？一句话本质
1. 原码、反码：**+0、-0 浪费了两个编码位置**
2. 补码：**0 只占一个位置**
3. 省下的那一个编码位，**拿来多表示一个负数**
   👉 所以补码比原码/反码**多一个数**（多了 -128）

---

## 三、位数通用规律（软考直接背）
n位二进制：
- 原码/反码范围：\(-(2^{n-1}-1)\) ~ \(+(2^{n-1}-1)\)
- 补码范围：\(-2^{n-1}\) ~ \(+(2^{n-1}-1)\)

举例：
- 8位：原反码 -127~127，补码 -128~127
- 16位：原反码 -32767~32767，补码 -32768~32767

---

## 四、额外好处（为什么计算机只用补码）
1. 0 唯一，不歧义
2. **减法可以变加法**，硬件不用单独做减法器
3. 编码利用率高，多表示一个负数


# 5. 1000 0000 规定代表 -128 是怎么转换的呢
## 一、8 位补码 `1000 0000` 表示什么？

先直接给答案：

```text id="u8m2qp"
8位补码：
1000 0000

表示：

-128
```

---

## 二、为什么是 -128？

先理解：

```text id="x5q7rv"
补码最高位是符号位
```

---

### 8 位二进制范围

8 位补码能表示：

```text id="m3n8tw"
-128 ~ 127
```

即：

| 类型  | 范围   |
| --- | ---- |
| 最小值 | -128 |
| 最大值 | 127  |

---

## 三、补码规则

---

### 正数

补码：

```text id="v7m1pk"
和原码一样
```

例如：

```text id="d4x8rv"
5

原码：
0000 0101

补码：
0000 0101
```

---

### 负数

补码规则：

```text id="g2q5tw"
符号位不变
其余位取反 + 1
```

---

## 四、验证 `1000 0000`

现在看：

```text id="n9m4rv"
1000 0000
```

最高位：

```text id="u1x7pk"
1
```

说明：

```text id="r5q2tw"
是负数
```

---

## 五、求它对应的十进制

补码转十进制：

---

### 第一步：减 1

```text id="z3m8rv"
1000 0000
↓

0111 1111
```

---

### 第二步：取反

```text id="f6q1pk"
0111 1111
↓

1000 0000
```

---

### 第三步：转十进制

```text id="k4m9tw"
1000 0000
=
128
```

所以：

```text id="h7x2rv"
结果是：

-128
```

---

## 六、为什么不是 “-0”？

这是补码最经典的问题。

---

### 原码会出现：

```text id="m1q8pk"
+0
-0
```

例如：

| 值  | 原码        |
| -- | --------- |
| +0 | 0000 0000 |
| -0 | 1000 0000 |

这样：

```text id="u5m4tw"
0 有两个表示
```

很麻烦。

---

## 七、补码如何解决？

补码规定：

```text id="n8x1rv"
1000 0000
不表示 -0
```

而表示：

```text id="r2q7pk"
最小值 -128
```

这样：

```text id="g9m3tw"
0 只有一个
```

即：

```text id="v4x8rv"
0000 0000
```

---

## 八、为什么范围是 -128 ~ 127？

8 位：

```text id="m7q2pk"
共 256 个状态
```

---

### 补码分配

| 范围   | 个数    |
| ---- | ----- |
| 负数   | 128 个 |
| 正数+0 | 128 个 |

---

### 所以：

```text id="x1m5tw"
负数多一个
```

即：

```text id="q6x9rv"
-128 ~ 127
```

---

## 九、8 位补码完整范围

| 二进制       | 十进制  |
| --------- | ---- |
| 0000 0000 | 0    |
| 0000 0001 | 1    |
| ...       | ...  |
| 0111 1111 | 127  |
| 1000 0000 | -128 |
| 1000 0001 | -127 |
| ...       | ...  |
| 1111 1111 | -1   |

---

## 十、为什么 `1111 1111` 是 -1？

验证一下：

---

### 补码

```text id="u3m7pk"
1111 1111
```

---

### 减1

```text id="h8q2tw"
1111 1110
```

---

### 取反

```text id="n4x9rv"
0000 0001
```

即：

```text id="z5m1pk"
1
```

所以：

```text id="r7q4tw"
结果：

-1
```

---

## 十一、补码最核心意义

补码最大的好处：

```text id="m2x8rv"
加减法统一
```

CPU：

```text id="g6q1pk"
不用专门处理减法
```

例如：

```text id="v9m5tw"
5 + (-3)
```

直接：

```text id="u1x7rv"
补码相加
```

即可。

---

## 十二、一句话总结

```text id="q4m8pk"
8位补码：

1000 0000

表示：

-128

它是8位补码能表示的最小值。
```

